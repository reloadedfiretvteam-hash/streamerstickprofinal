# Supabase Configuration
# Get these values from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Stripe Payment Integration
# Get these from: https://dashboard.stripe.com/apikeys
# Publishable key (starts with 'pk_test_' for testing or 'pk_live_' for production)
VITE_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
# Secret key - ONLY used in Supabase Edge Functions (starts with 'sk_test_' or 'sk_live_')
# Add this to your Supabase Edge Function secrets, NOT in your frontend .env file
# STRIPE_SECRET_KEY=your-stripe-secret-key

# CORS Configuration for Supabase Edge Functions
# Comma-separated list of allowed origins for CORS
# Example: https://yourdomain.com,https://www.yourdomain.com,https://pay.yourdomain.com
# Add this to your Supabase Edge Function environment variables
# ALLOWED_ORIGINS=https://streamstickpro.com,https://www.streamstickpro.com

# Optional: Stripe Payment Subdomain
# Comma-separated list of domains that should use Stripe checkout exclusively
VITE_STRIPE_HOSTS=pay.streamstickpro.com

# Optional: Concierge/Special Checkout Domains
# Comma-separated list of domains that should show concierge checkout
VITE_CONCIERGE_HOSTS=concierge.yourdomain.com

# Storage Bucket Configuration
# IMPORTANT: If your images are already uploaded to a Supabase bucket
# Specify the EXACT bucket name here. Common bucket names:
# - 'images' (recommended)
# - 'imiges' (old typo - if you have this, set it here)
# - 'product-images' (alternative name)
# Example: VITE_STORAGE_BUCKET_NAME=imiges
# If not set, defaults to 'images'
VITE_STORAGE_BUCKET_NAME=images
