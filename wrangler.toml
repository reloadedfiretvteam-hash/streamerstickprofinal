name = "streamerstickprofinal"
compatibility_date = "2024-11-02"
pages_build_output_dir = "dist"

# Cloudflare Pages Configuration
# Build settings are configured in the Cloudflare Pages Dashboard or via GitHub Actions
# Cache control is managed via public/_headers file
# Routing is managed via public/_routes.json file
